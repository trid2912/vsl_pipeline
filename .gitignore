# python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.env

# annotation secrets & uploads are also covered by annotation/.gitignore
annotation/auth/
annotation/sample_data/recording/videos/*
!annotation/sample_data/recording/videos/.gitkeep

# runtime data trees (live outside this repo; ignore if ever copied in)
/final_corpus/
/data/
/corpus_files/

# os / editor
.DS_Store
*.swp

# QC backups & intermediates (không đưa lên repo)
annotation/data/recording/*.csv.bak*
annotation/data/recording/_cfix_*.json
annotation/data/recording/historical_name_fixes.csv
annotation/data/recording/name_fixes_review.csv
